import { describe, it, expect } from 'vitest';

// Basic smoke test to keep Vitest happy until real UI tests are added.
describe('studio smoke test', () => {
  it('runs without errors', () => {
    expect(true).toBe(true);
  });
});
