import { describe, it, expect } from 'vitest';

describe('kflow vscode extension', () => {
	it('runs a placeholder test until real extension tests are added', () => {
		expect(true).toBe(true);
	});
});
